// ARRAY METHODS

const points =[40,100,1,5,25,10,25]
// points.forEach(value=>{
//     console.log(value)
// });

// points.forEach((value, index)=>{
//     console.log(value,index)
// });

// points.forEach((value, index, array)=>{
//     console.log(value,index,array)
// });

// points.forEach((a,b,c)=>{
//     //console.log(c[b]);
//      console.log(a)
// })

// points.forEach((value,index) =>{
//     console.log(index);
// });


// points.map((value,index)=>{
//     console.log(value,index);
//     });

    // const newArr=points.map((value,index)=>{
    //     console.log(value);
    // });


    //  const newArr=points.map((value,index) =>{
    //     return (value+=5);
    // });
    // console.log(newArr);
    
    //  const newArr=points.map((value,index)=>(value+=5));
    //  console.log(newArr);
       
    

    // const newArr=points.filter((value,index)=>(value>10));
    // console.log(newArr)
  
    //    const newArr=points.find((value,index)=>(value>10));
    // console.log(newArr)
  

    //   const newArr=points.reduce((sum,value)=>(sum+value));
    // console.log(newArr)
  
    //    const newArr=points.map((value,index)=>(value<5));
    //  console.log(newArr);

    